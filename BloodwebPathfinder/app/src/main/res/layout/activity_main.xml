<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewmodel"
            type="com.jacksonbcs.bloodwebpathfinder.main.WebViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".main.MainActivity"
        android:background="@color/background">

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/center_node"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:src="@drawable/center_node"
            android:contentDescription="@string/center_node_description" />

        <!--=== Innermost ring ===-->

        <!-- 12 o'clock -->
        <ImageView
            android:id="@+id/node_0_0"
            app:associatedVertex="@{viewmodel.vertex_0_0}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 2 o'clock -->
        <ImageView
            android:id="@+id/node_0_1"
            app:associatedVertex="@{viewmodel.vertex_0_1}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

        <!-- 4 o'clock -->
        <ImageView
            android:id="@+id/node_0_2"
            app:associatedVertex="@{viewmodel.vertex_0_2}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 6 o'clock -->
        <ImageView
            android:id="@+id/node_0_3"
            app:associatedVertex="@{viewmodel.vertex_0_3}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 8 o'clock -->
        <ImageView
            android:id="@+id/node_0_4"
            app:associatedVertex="@{viewmodel.vertex_0_4}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 10 o'clock -->
        <ImageView
            android:id="@+id/node_0_5"
            app:associatedVertex="@{viewmodel.vertex_0_5}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!--=== Middle ring ===-->

        <!-- Top right (12:30) -->
        <ImageView
            android:id="@+id/node_1_0"
            app:associatedVertex="@{viewmodel.vertex_1_0}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 2 o'clock -->
        <ImageView
            android:id="@+id/node_1_1"
            app:associatedVertex="@{viewmodel.vertex_1_1}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 2:30 -->
        <ImageView
            android:id="@+id/node_1_2"
            app:associatedVertex="@{viewmodel.vertex_1_2}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 3:30 -->
        <ImageView
            android:id="@+id/node_1_3"
            app:associatedVertex="@{viewmodel.vertex_1_3}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 4 o'clock -->
        <ImageView
            android:id="@+id/node_1_4"
            app:associatedVertex="@{viewmodel.vertex_1_4}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Bottom right (5:30) -->
        <ImageView
            android:id="@+id/node_1_5"
            app:associatedVertex="@{viewmodel.vertex_1_5}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Bottom left (6:30) -->
        <ImageView
            android:id="@+id/node_1_6"
            app:associatedVertex="@{viewmodel.vertex_1_6}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 8 o'clock -->
        <ImageView
            android:id="@+id/node_1_7"
            app:associatedVertex="@{viewmodel.vertex_1_7}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 8:30 -->
        <ImageView
            android:id="@+id/node_1_8"
            app:associatedVertex="@{viewmodel.vertex_1_8}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 9:30 -->
        <ImageView
            android:id="@+id/node_1_9"
            app:associatedVertex="@{viewmodel.vertex_1_9}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 10 o'clock -->
        <ImageView
            android:id="@+id/node_1_10"
            app:associatedVertex="@{viewmodel.vertex_1_10}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Top left (11:30) -->
        <ImageView
            android:id="@+id/node_1_11"
            app:associatedVertex="@{viewmodel.vertex_1_11}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!--=== Outermost ring ===-->

        <!-- 12 o'clock -->
        <ImageView
            android:id="@+id/node_2_0"
            app:associatedVertex="@{viewmodel.vertex_2_0}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 1 o'clock -->
        <ImageView
            android:id="@+id/node_2_1"
            app:associatedVertex="@{viewmodel.vertex_2_1}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 2 o'clock -->
        <ImageView
            android:id="@+id/node_2_2"
            app:associatedVertex="@{viewmodel.vertex_2_2}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 3 o'clock -->
        <ImageView
            android:id="@+id/node_2_3"
            app:associatedVertex="@{viewmodel.vertex_2_3}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 4 o'clock -->
        <ImageView
            android:id="@+id/node_2_4"
            app:associatedVertex="@{viewmodel.vertex_2_4}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 5 o'clock -->
        <ImageView
            android:id="@+id/node_2_5"
            app:associatedVertex="@{viewmodel.vertex_2_5}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 6 o'clock -->
        <ImageView
            android:id="@+id/node_2_6"
            app:associatedVertex="@{viewmodel.vertex_2_6}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 7 o'clock -->
        <ImageView
            android:id="@+id/node_2_7"
            app:associatedVertex="@{viewmodel.vertex_2_7}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 8 o'clock -->
        <ImageView
            android:id="@+id/node_2_8"
            app:associatedVertex="@{viewmodel.vertex_2_8}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 9 o'clock -->
        <ImageView
            android:id="@+id/node_2_9"
            app:associatedVertex="@{viewmodel.vertex_2_9}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 10 o'clock -->
        <ImageView
            android:id="@+id/node_2_10"
            app:associatedVertex="@{viewmodel.vertex_2_10}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 11 o'clock -->
        <ImageView
            android:id="@+id/node_2_11"
            app:associatedVertex="@{viewmodel.vertex_2_11}"
            style="@style/web_node"
            android:src="@drawable/node_ring"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>